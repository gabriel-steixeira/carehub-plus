/// Non-UI constants (timeouts, limits, etc.)
class AppConstants {
  AppConstants._();

  /// Duration the splash screen is displayed before navigating.
  static const Duration splashDuration = Duration(milliseconds: 2500);

  /// Minimum password length.
  static const int minPasswordLength = 6;
}
